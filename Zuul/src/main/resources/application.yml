eureka:
  instance:
    preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 9999
  use-forward-headers: true
spring:
  application:
    name: zuul
zuul:
  sensitive-headers:
---
spring:
  profiles: docker_env
eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka:8761/eureka/
zuul:
  sensitive-headers:
#  routes:
#    client:
#      path: /login
#      url: http://${ENV_HOST}:8585/secret/login